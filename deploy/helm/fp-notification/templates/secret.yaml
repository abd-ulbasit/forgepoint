{{- if .Values.secrets.create -}}
# ============================================================================
# Secret — SENSITIVE configuration (the Postgres DSN with password + the JWT
# verification key).
# ============================================================================
# ⚠️  PRODUCTION CONTRACT  ⚠️
# This rendered Secret is for LOCAL/DEV clusters ONLY. The values come from
# values.yaml placeholders, which must NEVER hold real credentials in git.
#
# In a real environment, set `secrets.create: false` and supply the Secret
# out-of-band so plaintext credentials never touch the chart or the repo:
#   - External Secrets Operator: an ExternalSecret CR syncs from AWS Secrets
#     Manager / HashiCorp Vault into a K8s Secret named `secrets.existingSecret`.
#   - Sealed Secrets: a SealedSecret CR (safe to commit — encrypted) that the
#     controller decrypts into the real Secret in-cluster.
# Either way the Deployment's envFrom references the SAME name
# (fp-notification.secretName), so flipping create on/off needs no other change.
#
# WHY two keys here (vs fp-auth which also has a bootstrap password): notification
# does NOT mint JWTs, but its gRPC auth interceptor must VERIFY caller JWTs locally
# (design D2), so it needs FP_JWT_SECRET — the SAME HMAC key the auth service signs
# with, here used verify-only. Plus FP_DATABASE_URL (the DB DSN). If the prod
# Redis/SMTP require auth, add FP_REDIS_URL / FP_SMTP_PASSWORD here too and drop them
# from the ConfigMap.
#
# WHY stringData (not data): stringData takes plaintext and Kubernetes
# base64-encodes it for us — no manual `base64` in templates, which is just
# obfuscation (base64 is NOT encryption) and a frequent source of corrupt
# secrets. The keys below become env vars via envFrom (FP_DATABASE_URL,
# FP_JWT_SECRET).
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fp-notification.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fp-notification.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Full Postgres DSN (database "fp_notification"); the password is the sensitive
  # part. The service refuses to start if unset (required).
  FP_DATABASE_URL: {{ required "secrets.databaseUrl is required when secrets.create=true" .Values.secrets.databaseUrl | quote }}
  # HMAC-SHA256 key the auth interceptor uses to VERIFY caller JWTs. Must match the
  # auth service's signing key and be >= 32 bytes; the service refuses to start if
  # unset (required). Lives in the Secret (never the ConfigMap) as sensitive key
  # material — same handling as fp-auth's FP_JWT_SECRET.
  FP_JWT_SECRET: {{ include "fp-notification.validateJwtSecret" . | quote }}
{{- end }}
