{{- if .Values.secrets.create -}}
# ============================================================================
# Secret — SENSITIVE configuration (the Postgres DSN with password).
# ============================================================================
# ⚠️  PRODUCTION CONTRACT  ⚠️
# This rendered Secret is for LOCAL/DEV clusters ONLY. The values come from
# values.yaml placeholders, which must NEVER hold real credentials in git.
#
# In a real environment, set `secrets.create: false` and supply the Secret
# out-of-band so plaintext credentials never touch the chart or the repo:
#   - External Secrets Operator: an ExternalSecret CR syncs from AWS Secrets
#     Manager / HashiCorp Vault into a K8s Secret named `secrets.existingSecret`.
#   - Sealed Secrets: a SealedSecret CR (safe to commit — encrypted) that the
#     controller decrypts into the real Secret in-cluster.
# Either way the Deployment's envFrom references the SAME name
# (fp-billing.secretName), so flipping create on/off needs no other change.
#
# BILLING vs AUTH: both carry FP_JWT_SECRET, but for different roles. Auth SIGNS
# tokens with it; billing only VERIFIES signatures with it (the auth interceptor's
# design-D2 local verify) and never signs. It MUST be the SAME key value as
# fp-auth's so the signatures auth produces validate here. Billing's other secret
# is its Postgres DSN. It is a SECRET (not a ConfigMap) because anyone who reads it
# can forge tokens billing would accept.
#
# WHY stringData (not data): stringData takes plaintext and Kubernetes
# base64-encodes it for us — no manual `base64` in templates, which is just
# obfuscation (base64 is NOT encryption) and a frequent source of corrupt
# secrets. The keys below become env vars via envFrom (FP_DATABASE_URL, FP_JWT_SECRET).
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fp-billing.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fp-billing.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Full Postgres DSN (database fp_billing); the password is the sensitive part.
  FP_DATABASE_URL: {{ required "secrets.databaseUrl is required when secrets.create=true" .Values.secrets.databaseUrl | quote }}
  # Shared HMAC-SHA256 VERIFY key for the auth interceptor (design D2 local verify).
  # The service refuses to start if unset (required) and rejects a secret < 32 bytes.
  # MUST equal fp-auth's secrets.jwtSecret so auth-issued tokens validate here.
  FP_JWT_SECRET: {{ required "secrets.jwtSecret is required when secrets.create=true (must match fp-auth's JWT secret; use an external secret store in prod)" .Values.secrets.jwtSecret | quote }}
{{- end }}
